name: CI/CD
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17', cache: maven }
      - run: mvn -B clean package
      - uses: actions/upload-artifact@v4
        with: { name: jar, path: target/student-management.jar }

  docker:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/student-management:latest

  deploy:
    needs: docker
    runs-on: ubuntu-latest
    steps:
      - name: SSH deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
               cd /opt/student-management

               docker compose pull

               docker compose up -d
          
        #  docker pull ${{ secrets.DOCKERHUB_USERNAME }}/student-management:latest
        #  docker stop sms || true && docker rm sms || true
        #  docker run -d --name sms --restart=always -p 8080:8080 \
          #    -e DB_HOST=${{ secrets.DB_HOST }} -e DB_USER=${{ secrets.DB_USER }} \
          #   -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} -e DB_NAME=studentdb \
          #    ${{ secrets.DOCKERHUB_USERNAME }}/student-management:latest
          

